# Required for live Jev selection through the bundled policy wrapper.
TYPESAFE_API_KEY=

# Optional path to the policy-enforcing Jev CLI wrapper.
JEV_CLIENT_PATH=

# Optional system Chromium path. Omit when Playwright manages its own browser.
PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=
